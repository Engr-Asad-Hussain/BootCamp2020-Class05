import React, {createContext} from 'react';

const SecondsContext = createContext(20);
export const MinutesContext = createContext(20);
export const HoursContext = createContext(20);

export default SecondsContext;